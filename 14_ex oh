def ExOh(str)

  # code goes here

  
  return str.scan(/["o"]/).count == str.scan(/["x"]/).count
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
ExOh(STDIN.gets)  
