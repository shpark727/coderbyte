def LetterCapitalize(str)

  # code goes here
  
  # &:method syntax in map  =>
  # a concise way to call a method on each item in the array.
  # join => to turn that array into a string
  ans = str.split.map(&:capitalize).join(' ')
  return ans
         
end
   
# keep this function call here 
# to see how to enter arguments in Ruby scroll down   
LetterCapitalize(STDIN.gets)  


















  
